import Calculadora from './Calculadora.js';
import { logger } from '../infraestrutura/logger.js';

test('Dado a soma da lista [1,2,3] deve retornar 6', () => {});
test('Dado a soma da lista [1,2,3] deve acrescentar 6 à variável total', () => {});
test('Dado a soma da lista c[4,5,6] deve chamar o info do logger com valor "total atualizado: 15"', () => {});
